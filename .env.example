# -----------------------------------------------------
# MANTRA-MD ENVIRONMENT CONFIGURATION
# Rename this file to .env for local use
# or add these Key/Values to your Cloud Dashboard (Railway/Heroku/Render)
# -----------------------------------------------------

# 1. SESSION ID
# If you have a saved session string (starts with Mantra~), paste it here.
# If empty, the bot will start in pairing mode (uses phone number in config.js).
SESSION_ID=

# 2. OWNER SETTINGS
# Your WhatsApp number (e.g., 2547XXXXXXXX)
OWNER_NUMBER=254732647560

# 3. BOT METADATA
BOT_NAME=Mantra-MD
AUTHOR_NAME=MidknightMantra

# 4. DEPLOYMENT PORT
# Required for Render/Railway/Heroku to keep the bot alive.
# Default is usually 8080 or 3000.
PORT=8080

# 5. NODE ENVIRONMENT
# Set to 'production' to suppress detailed debug logs
NODE_ENV=production

# ============================================
# DATABASE CONFIGURATION
# ============================================
# The bot automatically detects which database to use:
# - If DATABASE_URL is set → Uses MongoDB
# - If DATABASE_URL is empty → Uses JSON file (database.json)

# OPTION 1: MongoDB (Production - Recommended for cloud deployment)
# Get free MongoDB cluster from: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
DATABASE_URL=

# OPTION 2: JSON File (Development - Automatic fallback)
# Leave DATABASE_URL empty to use local JSON file storage
# File: ./database.json (created automatically)

# ============================================
# REDIS CONFIGURATION (Optional - for caching)
# ============================================
# Leave empty to disable Redis caching
# The bot will work fine without Redis (graceful degradation)

REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# API KEYS (Optional)
# ============================================
# Some plugins require API keys for full functionality

# OpenWeather API (for weather command)
# Get free key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=

# ============================================
# PERFORMANCE TUNING (Optional)
# ============================================
# Message cache size for anti-delete (default: 500)
# Lower this if you have limited RAM (min: 100, max: 2000)
# Each message uses ~2-5 KB, so 500 msgs = ~1-2.5 MB RAM
MESSAGE_CACHE_SIZE=500
